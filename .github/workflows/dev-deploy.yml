name: Deploy to Development

on:
  push:
    branches:
      - development

jobs:
  deploy-dev:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: Simulate Development Deployment
        run: echo "✅ Code pushed to development branch. Deploying to Development Environment..."
